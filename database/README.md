# StudyTimer Database Schemas

This directory contains SQL scripts and schema definitions for Supabase:

- `schema_analytics.sql`: Analytics, events, and session tracking database tables and security policies.
- `fix_delete_permissions.sql`: Row-level security (RLS) and delete policy adjustments for GDPR / data deletion compliance.
